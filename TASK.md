# Уровень 1 — «Квалификация»

## Цель
Провести робота от старта через 4 чекпоинта **по порядку** и удержаться на финише. Это
разминочный уровень: научиться ехать, держать курс и проходить повороты, не падая.

## Трасса
Старт в начале коридора. S-образная шикана (обход двух стенок-перегородок), затем «змейка» из
нескольких прямых с поворотами на 90°. Чекпоинты `cp1…cp4` расставлены вдоль правильного
маршрута; финишное кольцо — в конце.

## Что редактировать
`level1/controllers/participant/participant.py` — единственный файл. Стартовый каркас просто
едет вперёд и упирается в стену; доведи робота до финиша сам.

## Очки
- 4 чекпоинта × 10 = **40** (режим *sequential*: берутся строго по порядку; финиш открывается
  после `cp4`).
- Финиш: доехать до кольца (радиус 0.3 м) и удержаться **3 с**.
- **Бонус +30%** за полное прохождение (все 4 чекпоинта + финиш).
- **Максимум — 52 очка.**

## Провал
- Падение/переворот: корпус ниже `z = 0.12` м или крен больше `60°`.
- Таймаут: **240 с** симуляции.

## Подсказки
- Используй `robot.lidar_layer()` и `robot.pose()`, а не хардкод точек — так надёжнее на поворотах.
- Держи ~0.4–0.5 м/с; на поворотах сбавляй, чтобы не занесло в стену.
- Ранжирование — по времени симуляции (`robot.time()`): чем быстрее финиш, тем лучше.
