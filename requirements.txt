# Зависимости локомоции робота: штатная политика ходьбы Go2 крутится в питоне контроллера.
# Колёса есть под Windows (win_amd64), macOS (Intel+Apple Silicon) и Linux, Python 3.10–3.13.
numpy>=1.24,<3
MNN>=3.0,<4

# Необязательно — только если строишь Go2(enable_ros2=True):
# rclpy
